////Example with lodash
//import * as _ from 'lodash'
//var data = _.map([1, 2, 3], function (num) { return num * 3; });
//Console.AppendText(JSON.stringify(data));

//Example with underscore
import * as _ from 'underscore-esm'
var data = _.map([1, 2, 3], function (num) { return num * 3; });
Console.AppendText(JSON.stringify(data));
